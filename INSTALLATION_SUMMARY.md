# 📋 Резюме: Развертывание сайта на VDS

## 🎯 Цель
Развернуть Next.js приложение на VDS сервере с доменом `derevo.space`

## 📊 Данные сервера
- **IP:** 193.32.188.93
- **Логин:** root
- **Пароль:** Zt7Ncu36feJh
- **Домен:** derevo.space

## 🚀 Быстрый план действий

### 1. Подключение к серверу
```bash
ssh root@193.32.188.93
```

### 2. Автоматическая установка ПО
```bash
# Загрузите setup-server.sh на сервер
bash setup-server.sh
```

### 3. Загрузка проекта
```bash
# С вашего компьютера:
rsync -avz --exclude 'node_modules' --exclude '.next' \
  ./ root@193.32.188.93:/var/www/derevo/
```

### 4. Настройка переменных окружения
```bash
# На сервере:
cd /var/www/derevo
nano .env.production
# Заполните переменные из Supabase
```

### 5. Запуск приложения
```bash
cd /var/www/derevo
chmod +x deploy.sh
./deploy.sh
```

### 6. Настройка Nginx
```bash
cp /var/www/derevo/nginx.conf /etc/nginx/sites-available/derevo
ln -s /etc/nginx/sites-available/derevo /etc/nginx/sites-enabled/
rm /etc/nginx/sites-enabled/default
nginx -t
systemctl restart nginx
```

### 7. SSL сертификат (опционально)
```bash
apt install certbot python3-certbot-nginx -y
certbot --nginx -d derevo.space -d www.derevo.space
```

## 📁 Созданные файлы

### Для деплоя:
- ✅ `Dockerfile` - Docker образ
- ✅ `docker-compose.yml` - Конфигурация контейнеров
- ✅ `nginx.conf` - Конфигурация веб-сервера
- ✅ `deploy.sh` - Скрипт автоматического деплоя
- ✅ `setup-server.sh` - Скрипт установки ПО на сервере

### Документация:
- ✅ `DEPLOY.md` - Подробная инструкция
- ✅ `QUICK_START.md` - Краткая инструкция
- ✅ `README_DEPLOY.md` - Описание файлов
- ✅ `.env.production.example` - Пример переменных окружения

### Изменения в проекте:
- ✅ `next.config.js` - Добавлен `output: 'standalone'`

## ⚠️ Важно!

1. **Переменные окружения:**
   - Создайте `.env.production` на сервере
   - Получите значения из Supabase Dashboard
   - Не коммитьте файл с реальными значениями

2. **Домен:**
   - Убедитесь, что `derevo.space` указывает на IP `193.32.188.93`
   - Настройте A-запись в DNS

3. **Безопасность:**
   - Настройте SSL (HTTPS)
   - Настройте файрвол
   - Регулярно обновляйте систему

## 📞 Проверка работы

После деплоя проверьте:
1. `http://derevo.space` - должен открываться сайт
2. `docker-compose ps` - контейнеры должны быть запущены
3. `docker-compose logs -f` - проверьте логи на ошибки

## 🔄 Обновление

Для обновления кода:
```bash
cd /var/www/derevo
git pull  # или загрузите новые файлы
./deploy.sh
```

## 📖 Подробные инструкции

- **QUICK_START.md** - для быстрого старта
- **DEPLOY.md** - для детальной настройки
- **README_DEPLOY.md** - описание всех файлов

---

**Готово к развертыванию! 🎉**


