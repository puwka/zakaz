# 🔧 Решение проблемы с SSH подключением

## 📊 Текущая ситуация

Судя по скриншоту:
- ✅ SSH сервис **запущен** и работает на сервере
- ✅ Файрвол (ufw) **настроен** и порт 22 открыт
- ❌ Подключение с вашего компьютера **не проходит** (Connection timed out)

**Важно:** В логах видны попытки подключения с других IP адресов, значит SSH доступен извне, но не с вашего IP.

---

## 🔍 Возможные причины

### 1. Файрвол Proxmox блокирует подключения

Proxmox имеет свой файрвол, который может блокировать входящие соединения к VM.

**Решение:**

1. **Войдите в веб-интерфейс Proxmox:**
   - Откройте: https://ru202.vdska.ru:8006
   - Войдите с учетными данными

2. **Найдите вашу VM:**
   - В левой панели найдите вашу виртуальную машину
   - Кликните на неё

3. **Проверьте файрвол Proxmox:**
   - Перейдите в раздел **"Firewall"** (Файрвол)
   - Проверьте правила файрвола
   - Убедитесь, что порт **22 (SSH)** разрешен

4. **Добавьте правило файрвола (если нужно):**
   - В разделе "Firewall" → "Rules"
   - Нажмите **"Add"** (Добавить)
   - **Action:** `ACCEPT`
   - **Protocol:** `tcp`
   - **Destination port:** `22`
   - **Comment:** `SSH access`
   - Нажмите **"Add"**

5. **Проверьте, включен ли файрвол на уровне VM:**
   - В разделе "Firewall" убедитесь, что файрвол **включен** для VM
   - Если файрвол включен, но правила не работают - попробуйте временно отключить файрвол на уровне VM для проверки

---

### 2. Сетевые настройки VM в Proxmox

Возможно, VM не имеет прямого доступа к внешней сети.

**Решение:**

1. **Проверьте сетевые настройки VM:**
   - В Proxmox найдите вашу VM
   - Перейдите в раздел **"Hardware"** (Оборудование)
   - Найдите сетевой интерфейс (обычно "net0")
   - Кликните на него → **"Edit"** (Редактировать)

2. **Проверьте настройки:**
   - **Bridge:** должен быть выбран правильный сетевой мост
   - **Model:** обычно `VirtIO` (паравиртуализация)
   - Убедитесь, что интерфейс **включен**

3. **Проверьте IP адрес VM:**
   - В разделе **"Summary"** проверьте IP адрес VM
   - Убедитесь, что IP адрес правильный: `193.32.188.93`
   - Если IP другой - используйте правильный IP для подключения

---

### 3. Файрвол провайдера блокирует порт 22

Некоторые провайдеры VDS блокируют порт 22 по умолчанию.

**Решение:**

1. **Проверьте панель управления провайдера:**
   - Войдите в панель управления вашего VDS провайдера
   - Найдите раздел "Файрвол" или "Firewall"
   - Убедитесь, что порт **22 (SSH)** открыт

2. **Если нет доступа к панели провайдера:**
   - Свяжитесь с поддержкой провайдера
   - Попросите открыть порт 22 для вашего IP адреса или для всех

---

### 4. Проблемы с сетью на вашем компьютере

Возможно, ваш файрвол или антивирус блокирует исходящие SSH соединения.

**Решение:**

1. **Проверьте файрвол Windows:**
   - Откройте "Брандмауэр Защитника Windows"
   - Проверьте правила для исходящих соединений
   - Убедитесь, что SSH (порт 22) не заблокирован

2. **Проверьте антивирус:**
   - Временно отключите антивирус
   - Попробуйте подключиться снова
   - Если работает - добавьте исключение в антивирус

3. **Проверьте с другого компьютера/сети:**
   - Попробуйте подключиться с другого компьютера
   - Или используйте мобильный интернет (телефон как точка доступа)
   - Это поможет определить, проблема на вашей стороне или на стороне сервера

---

### 5. NAT/Маршрутизация в Proxmox

Возможно, VM находится за NAT и нужен порт-форвардинг.

**Решение:**

1. **Проверьте, какой IP у VM:**
   ```bash
   # Через веб-консоль Proxmox выполните:
   ip addr show
   # или
   hostname -I
   ```

2. **Проверьте, публичный ли IP:**
   - Если IP начинается с `10.`, `172.16-31.`, `192.168.` - это приватный IP
   - В этом случае нужна настройка порт-форвардинга в Proxmox

3. **Настройте порт-форвардинг (если нужно):**
   - В Proxmox найдите вашу VM
   - Перейдите в раздел "Hardware" → "Network"
   - Настройте порт-форвардинг для порта 22

---

## ✅ Рекомендуемое решение

### Вариант 1: Использовать веб-консоль Proxmox (САМЫЙ ПРОСТОЙ)

Если SSH не работает, используйте веб-консоль Proxmox - это самый надежный способ:

1. **Откройте Proxmox:**
   - https://ru202.vdska.ru:8006

2. **Найдите вашу VM → Console → noVNC**

3. **Работайте через веб-консоль:**
   - Все команды работают так же, как в SSH
   - Не требует открытого порта 22
   - Работает из любого браузера

**Это самый простой и надежный способ!** 🎯

---

### Вариант 2: Настроить файрвол Proxmox

Если хотите использовать SSH:

1. **Войдите в Proxmox:** https://ru202.vdska.ru:8006

2. **Найдите вашу VM → Firewall**

3. **Добавьте правило:**
   - Action: `ACCEPT`
   - Protocol: `tcp`
   - Destination port: `22`

4. **Сохраните и попробуйте подключиться снова**

---

### Вариант 3: Использовать другой порт SSH

Если порт 22 заблокирован провайдером:

1. **Через веб-консоль Proxmox измените порт SSH:**
   ```bash
   # Отредактируйте конфигурацию SSH
   nano /etc/ssh/sshd_config
   
   # Найдите строку #Port 22
   # Раскомментируйте и измените на другой порт, например:
   Port 2222
   
   # Сохраните (Ctrl+O, Enter, Ctrl+X)
   
   # Перезапустите SSH
   systemctl restart ssh
   ```

2. **Откройте новый порт в файрволе:**
   ```bash
   ufw allow 2222/tcp
   ```

3. **Настройте файрвол Proxmox для нового порта**

4. **Подключайтесь с указанием порта:**
   ```bash
   ssh -p 2222 root@193.32.188.93
   ```

---

## 🔍 Диагностика

### Проверка с сервера (через веб-консоль Proxmox):

```bash
# 1. Проверьте, что SSH слушает порт 22
netstat -tulpn | grep :22
# или
ss -tulpn | grep :22

# 2. Проверьте статус SSH
systemctl status ssh

# 3. Проверьте файрвол
ufw status verbose

# 4. Проверьте логи SSH
tail -f /var/log/auth.log
# или
journalctl -u ssh -f
```

### Проверка с вашего компьютера:

```powershell
# В PowerShell
Test-NetConnection -ComputerName 193.32.188.93 -Port 22

# Проверка ping
ping 193.32.188.93

# Проверка через telnet (если установлен)
telnet 193.32.188.93 22
```

---

## 📞 Что делать дальше

1. **Попробуйте веб-консоль Proxmox** - это самый простой способ
2. **Проверьте файрвол Proxmox** - добавьте правило для порта 22
3. **Свяжитесь с поддержкой провайдера** - попросите открыть порт 22
4. **Используйте другой порт SSH** - если порт 22 заблокирован

---

## 💡 Важно

**Веб-консоль Proxmox работает всегда**, даже если SSH недоступен. Это самый надежный способ работы с сервером!

Все команды из инструкции по установке можно выполнять через веб-консоль - результат будет такой же, как при использовании SSH.


