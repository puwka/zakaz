# 🖥️ Полная инструкция: Установка через веб-консоль Proxmox

## 🎯 Эта инструкция покажет, как установить сайт полностью через веб-консоль Proxmox

**Преимущества веб-консоли:**
- ✅ Не требует SSH подключения
- ✅ Работает из любого браузера
- ✅ Все команды выполняются так же, как в терминале
- ✅ Можно копировать/вставлять команды

---

## 📝 Шаг 1: Подключение к веб-консоли Proxmox

### 1.1. Откройте веб-интерфейс Proxmox

1. Откройте браузер (Chrome, Firefox, Edge)
2. Перейдите по адресу: **https://ru202.vdska.ru:8006**
3. Войдите с вашими учетными данными Proxmox

> ⚠️ Если не знаете учетные данные - уточните у провайдера VDS

### 1.2. Найдите вашу виртуальную машину

1. В **левой панели** найдите список виртуальных машин
2. Найдите вашу VM (виртуальную машину)
3. **Убедитесь, что она запущена:**
   - Статус должен быть **"Running"** (зеленый индикатор)
   - Если статус "Stopped" - нажмите кнопку **"Start"** (Запустить) вверху

### 1.3. Откройте консоль VM

1. **Кликните** на вашу виртуальную машину в левой панели
2. В верхнем меню выберите раздел **"Console"** (Консоль)
3. Выберите тип консоли:
   - **"noVNC"** - рекомендуется (лучше работает в браузере)
   - **"xterm.js"** - альтернативный вариант
4. Откроется окно с консолью виртуальной машины

### 1.4. Войдите в систему

В открывшейся консоли:

1. Введите логин: `root`
2. Нажмите **Enter**
3. Введите пароль: `Zt7Ncu36feJh`
4. Нажмите **Enter**

> ⚠️ При вводе пароля символы могут не отображаться - это нормально. Просто введите пароль и нажмите Enter.

**Проверка подключения:**
```bash
whoami
# Должно вывести: root

pwd
# Должно вывести: /root
```

---

## 🔧 Шаг 2: Установка необходимого ПО

Все команды выполняйте в веб-консоли Proxmox.

### 2.1. Обновление системы

Скопируйте и вставьте эту команду в консоль:

```bash
apt update && apt upgrade -y
```

**Что происходит:**
- Обновляется список пакетов
- Устанавливаются обновления системы
- Это может занять **2-5 минут**

**⚠️ ВАЖНО: Во время выполнения команды:**
- На экране будет появляться текст о процессе обновления
- Могут быть вопросы типа `[Y/n]` - нажмите `Y` и Enter
- **НЕ закрывайте консоль и НЕ прерывайте процесс!**

**Ожидаемый результат:** 
- В конце будет `Reading package lists... Done`
- Или `0 upgraded, 0 newly installed` (если все уже обновлено)
- Команда вернется в командную строку (появится `root@vdska:~#`)

### 2.2. Установка Docker

#### Вариант А: Установка через официальный скрипт (если есть интернет)

Выполните команды по порядку:

```bash
curl -fsSL https://get.docker.com -o get-docker.sh
```

**Ожидаемый результат:** Команда выполнится быстро, вернется в командную строку.

**Если получили ошибку `Timeout` или `Failed to connect`:**
- Проблема с интернет-соединением или DNS
- Переходите к **Варианту Б** (установка через apt)

Теперь запустите установку:

```bash
sh get-docker.sh
```

#### Вариант Б: Установка через apt (если нет интернета или проблемы с DNS)

Если вариант А не работает, используйте установку через пакетный менеджер:

```bash
# Обновим список пакетов
apt update
```

```bash
# Установим необходимые пакеты
apt install -y ca-certificates curl gnupg lsb-release
```

```bash
# Добавим официальный GPG ключ Docker
mkdir -p /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/debian/gpg | gpg --dearmor -o /etc/apt/keyrings/docker.gpg
```

**Если эта команда тоже не работает, попробуйте альтернативный способ:**

```bash
# Установим Docker напрямую из репозитория Debian
apt install -y docker.io docker-compose
```

**Или попробуйте установить через альтернативный источник:**

```bash
# Добавим репозиторий Docker
echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/debian $(lsb_release -cs) stable" | tee /etc/apt/sources.list.d/docker.list > /dev/null
```

```bash
# Обновим список пакетов
apt update
```

```bash
# Установим Docker
apt install -y docker-ce docker-ce-cli containerd.io docker-compose-plugin
```

**⚠️ ВАЖНО: После выполнения этой команды может показаться, что ничего не происходит!**

**Это нормально!** Установка Docker занимает **3-5 минут**. В это время:
- ✅ Процесс работает в фоне
- ✅ Устанавливаются пакеты Docker
- ✅ На экране может не быть вывода (это нормально)
- ✅ **НЕ закрывайте консоль и НЕ прерывайте процесс!**

**Как понять, что процесс идет:**
- Курсор мигает внизу экрана
- Консоль не отвечает на команды (это нормально)
- Может появиться текст с информацией об установке

**Подождите 3-5 минут.** В конце вы увидите:
```
Docker is installed!
```

**Если прошло больше 10 минут и ничего не происходит:**
1. Нажмите `Ctrl + C` (прервать)
2. Попробуйте еще раз: `sh get-docker.sh`
3. Или проверьте интернет-соединение

**После успешной установки** удалите временный файл:

```bash
rm get-docker.sh
```

### 2.3. Установка Docker Compose

```bash
apt install docker-compose -y
```

**Ожидаемый результат:** `docker-compose is already the newest version` или успешная установка

### 2.4. Установка Nginx

```bash
apt install nginx -y
```

```bash
systemctl enable nginx
```

```bash
systemctl start nginx
```

**Ожидаемый результат:** Команды выполнятся без ошибок

### 2.5. Проверка установки

Проверьте, что все установлено правильно:

```bash
docker --version
```

**Ожидаемый результат:** `Docker version 24.x.x` или похожее

```bash
docker-compose --version
```

**Ожидаемый результат:** `docker-compose version 1.x.x` или похожее

```bash
nginx -v
```

**Ожидаемый результат:** `nginx version: nginx/1.x.x`

**Если все команды показали версии - отлично! Переходите к следующему шагу.**

---

## 📦 Шаг 3: Загрузка проекта на сервер

### 3.1. Создайте директорию для проекта

```bash
mkdir -p /var/www/derevo
```

```bash
cd /var/www/derevo
```

### 3.2. Загрузите файлы проекта

**У вас есть несколько вариантов:**

#### Вариант А: Если у вас есть Git репозиторий

```bash
# Замените <ваш-репозиторий> на URL вашего репозитория
git clone <ваш-репозиторий> /var/www/derevo
```

```bash
cd /var/www/derevo
```

#### Вариант Б: Загрузка через веб-интерфейс Proxmox (File Manager)

1. **В веб-интерфейсе Proxmox:**
   - Найдите вашу VM
   - Перейдите в раздел **"Storage"** (Хранилище)
   - Или используйте раздел **"Backup"** → **"Upload"**

2. **Альтернатива:** Используйте WinSCP или другой SFTP клиент:
   - Хост: `193.32.188.93`
   - Пользователь: `root`
   - Пароль: `Zt7Ncu36feJh`
   - Протокол: `SFTP`
   - Загрузите все файлы проекта в `/var/www/derevo`

#### Вариант В: Создание файлов вручную (если нужно)

Если у вас нет доступа к файлам проекта, можно создать их через веб-консоль:

```bash
# Создайте файл через nano
nano test.txt
# Введите текст, нажмите Ctrl+O (сохранить), Enter, Ctrl+X (выйти)
```

**После загрузки файлов проверьте:**

```bash
cd /var/www/derevo
ls -la
```

**Должны быть видны файлы проекта:** `package.json`, `Dockerfile`, `docker-compose.yml` и т.д.

---

## 🔐 Шаг 4: Настройка переменных окружения

### 4.1. Создайте файл с настройками

```bash
cd /var/www/derevo
```

```bash
nano .env.production
```

**Откроется редактор nano.** Вставьте следующее содержимое:

```env
NEXT_PUBLIC_SUPABASE_URL=https://ваш-проект.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш-anon-key
SUPABASE_SERVICE_ROLE_KEY=ваш-service-role-key
TELEGRAM_BOT_TOKEN=ваш-telegram-bot-token
TELEGRAM_CHAT_ID=ваш-telegram-chat-id
```

**Где взять эти значения:**

1. **Supabase:**
   - Откройте: https://supabase.com/dashboard
   - Выберите ваш проект
   - Перейдите в **Settings** → **API**
   - Скопируйте:
     - **Project URL** → `NEXT_PUBLIC_SUPABASE_URL`
     - **anon public** key → `NEXT_PUBLIC_SUPABASE_ANON_KEY`
     - **service_role** key → `SUPABASE_SERVICE_ROLE_KEY` ⚠️ **секретный ключ!**

2. **Telegram (если используете):**
   - Создайте бота через @BotFather в Telegram
   - Получите токен → `TELEGRAM_BOT_TOKEN`
   - Узнайте ваш Chat ID → `TELEGRAM_CHAT_ID`

### 4.2. Сохраните файл

В редакторе nano:

1. **Замените** `ваш-проект.supabase.co`, `ваш-anon-key` и т.д. на реальные значения
2. Нажмите **`Ctrl + O`** (сохранить)
3. Нажмите **`Enter`** (подтвердить имя файла)
4. Нажмите **`Ctrl + X`** (выйти)

**Проверка:**

```bash
cat .env.production
```

**Должны быть видны ваши переменные окружения (без реальных ключей в выводе).**

---

## 🚀 Шаг 5: Запуск приложения

### 5.1. Убедитесь, что вы в правильной директории

```bash
cd /var/www/derevo
pwd
# Должно вывести: /var/www/derevo
```

### 5.2. Сделайте скрипт исполняемым (если есть deploy.sh)

```bash
chmod +x deploy.sh
```

### 5.3. Запустите деплой

**Если есть скрипт deploy.sh:**

```bash
./deploy.sh
```

**⚠️ ВАЖНО: Это займет 5-10 минут!**
- На экране будет появляться текст о процессе сборки
- Может показаться, что процесс "завис" - это нормально
- **НЕ закрывайте консоль и НЕ прерывайте процесс!**
- Дождитесь завершения (команда вернется в командную строку)

**Если скрипта нет, выполните вручную:**

```bash
# Соберите Docker образ
docker-compose build
```

**⚠️ ВАЖНО: Это займет 5-10 минут!**
- На экране будет появляться текст о процессе сборки
- Может показаться, что процесс "завис" - это нормально
- **НЕ закрывайте консоль и НЕ прерывайте процесс!**
- Дождитесь завершения (команда вернется в командную строку)

```bash
# Запустите контейнер
docker-compose up -d
```

**Эта команда выполнится быстрее (30 секунд - 1 минута).**

**Проверка:**

```bash
docker-compose ps
```

**Ожидаемый результат:** Контейнер должен быть в статусе `Up`

```bash
# Проверьте, что приложение работает
curl http://localhost:3000
```

**Ожидаемый результат:** Должен вернуться HTML код страницы

---

## 🌐 Шаг 6: Настройка веб-сервера (Nginx)

### 6.1. Скопируйте конфигурацию

```bash
cd /var/www/derevo
```

```bash
cp nginx.conf /etc/nginx/sites-available/derevo
```

### 6.2. Включите конфигурацию

```bash
ln -s /etc/nginx/sites-available/derevo /etc/nginx/sites-enabled/
```

```bash
rm /etc/nginx/sites-enabled/default
```

### 6.3. Проверьте конфигурацию

```bash
nginx -t
```

**Ожидаемый результат:**
```
nginx: the configuration file /etc/nginx/nginx.conf syntax is ok
nginx: configuration file /etc/nginx/nginx.conf test is successful
```

**Если есть ошибки - проверьте файл конфигурации.**

### 6.4. Перезапустите Nginx

```bash
systemctl restart nginx
```

**Проверка:**

```bash
systemctl status nginx
```

**Ожидаемый результат:** `Active: active (running)`

---

## 🔒 Шаг 7: Настройка SSL (HTTPS) - Рекомендуется

### 7.1. Установите Certbot

```bash
apt install certbot python3-certbot-nginx -y
```

### 7.2. Получите SSL сертификат

```bash
certbot --nginx -d derevo.space -d www.derevo.space
```

**Следуйте инструкциям на экране:**

1. **Введите email** (для уведомлений о продлении сертификата)
2. **Согласитесь с условиями:** введите `Y` и нажмите Enter
3. **Выберите редирект с HTTP на HTTPS:** выберите `2` (Redirect) и нажмите Enter

**Ожидаемый результат:** `Congratulations! Your certificate and chain have been saved...`

### 7.3. Проверьте автоматическое обновление

```bash
certbot renew --dry-run
```

**Ожидаемый результат:** `The dry run was successful.`

---

## ✅ Шаг 8: Проверка работы

### 8.1. Откройте сайт в браузере

Перейдите по адресу: **http://derevo.space** (или **https://derevo.space** после настройки SSL)

**Сайт должен открыться!** 🎉

### 8.2. Проверьте логи (если что-то не работает)

**Логи приложения:**

```bash
cd /var/www/derevo
docker-compose logs -f
```

**Чтобы выйти из просмотра логов:** нажмите `Ctrl + C`

**Логи Nginx:**

```bash
tail -f /var/log/nginx/derevo-access.log
```

```bash
tail -f /var/log/nginx/derevo-error.log
```

**Чтобы выйти:** нажмите `Ctrl + C`

---

## 🔄 Обновление сайта (когда нужно обновить код)

### Вариант А: Через Git

```bash
cd /var/www/derevo
git pull
./deploy.sh
```

### Вариант Б: Загрузить новые файлы

1. Загрузите новые файлы на сервер (через WinSCP или другой SFTP клиент)
2. Затем в веб-консоли:

```bash
cd /var/www/derevo
./deploy.sh
```

---

## 🛠️ Полезные команды

### Просмотр статуса контейнеров

```bash
cd /var/www/derevo
docker-compose ps
```

### Просмотр логов приложения

```bash
docker-compose logs -f
```

**Чтобы выйти:** `Ctrl + C`

### Перезапуск приложения

```bash
docker-compose restart
```

### Остановка приложения

```bash
docker-compose down
```

### Запуск приложения

```bash
docker-compose up -d
```

### Просмотр использования ресурсов

```bash
docker stats
```

**Чтобы выйти:** `Ctrl + C`

---

## ❗ Решение проблем

### Проблема: Сайт не открывается

**Решение:**

1. **Проверьте, что контейнер запущен:**
   ```bash
   cd /var/www/derevo
   docker-compose ps
   ```
   Контейнер должен быть в статусе `Up`

2. **Проверьте логи:**
   ```bash
   docker-compose logs
   ```
   Ищите ошибки в логах

3. **Проверьте Nginx:**
   ```bash
   systemctl status nginx
   ```
   Должен быть `Active: active (running)`

4. **Проверьте, что домен указывает на правильный IP:**
   ```bash
   nslookup derevo.space
   ```
   Должен вернуться IP `193.32.188.93`

### Проблема: Ошибки при сборке Docker

**Решение:**

1. **Проверьте файл `.env.production`:**
   ```bash
   cat .env.production
   ```
   Убедитесь, что все переменные заполнены правильно

2. **Проверьте логи сборки:**
   ```bash
   docker-compose build --no-cache
   ```
   Ищите ошибки в выводе

3. **Проверьте доступное место на диске:**
   ```bash
   df -h
   ```
   Если диск заполнен - освободите место

### Проблема: Проблемы с Nginx

**Решение:**

1. **Проверьте конфигурацию:**
   ```bash
   nginx -t
   ```
   Исправьте ошибки, если есть

2. **Проверьте логи:**
   ```bash
   tail -f /var/log/nginx/error.log
   ```
   Ищите ошибки в логах

3. **Перезапустите Nginx:**
   ```bash
   systemctl restart nginx
   ```

### Проблема: Не могу скопировать/вставить команды в веб-консоль

**Решение:**

- **В noVNC:** Используйте контекстное меню (правый клик) → "Paste"
- **В xterm.js:** Используйте `Ctrl + Shift + V` для вставки
- **Альтернатива:** Вводите команды вручную

### Проблема: Консоль зависла или не отвечает

**⚠️ ВАЖНО: Сначала убедитесь, что это не просто долгая установка!**

**Проверка:**
- Если выполняется команда установки (Docker, apt, docker-compose build) - **это нормально!**
- Установка может занять 3-10 минут
- Курсор должен мигать внизу экрана
- **Подождите минимум 10 минут перед тем, как считать, что что-то не так**

**Если действительно зависла (прошло больше 10 минут):**

1. **Попробуйте прервать процесс:**
   - Нажмите `Ctrl + C`
   - Подождите 10 секунд
   - Если не помогло - переходите к следующему шагу

2. **Обновите страницу браузера** (F5)
3. **Переподключитесь к консоли:**
   - Закройте текущую консоль
   - Откройте консоль заново (Console → noVNC)
4. **Попробуйте другой тип консоли** (noVNC или xterm.js)

### Проблема: Ошибка подключения к get.docker.com (Timeout, Failed to connect)

**Симптомы:**
- `curl: (28) Failed to connect to get.docker.com port 443`
- `Timeout was reached`
- `Connection refused`

**Причины:**
- Проблемы с интернет-соединением на сервере
- Проблемы с DNS
- Файрвол блокирует исходящие соединения

**Решение:**

1. **Проверьте интернет-соединение:**
   ```bash
   ping -c 4 8.8.8.8
   ```
   Если ping не проходит - проблема с интернетом на сервере

2. **Проверьте DNS:**
   ```bash
   nslookup get.docker.com
   ```
   Если не резолвится - проблема с DNS

3. **Попробуйте установить Docker через apt (Вариант Б):**
   ```bash
   apt update
   apt install -y docker.io docker-compose
   ```
   Это установит Docker из репозитория Debian (не требует подключения к get.docker.com)

4. **Проверьте файрвол:**
   ```bash
   ufw status
   ```
   Если файрвол активен, возможно он блокирует исходящие соединения

5. **Проверьте настройки сети в Proxmox:**
   - Войдите в Proxmox → найдите вашу VM
   - Проверьте раздел "Hardware" → "Network"
   - Убедитесь, что сетевой интерфейс настроен правильно

6. **Обратитесь к провайдеру:**
   - Если ничего не помогает, возможно провайдер блокирует исходящие соединения
   - Попросите проверить настройки сети и файрвола

### Проблема: Команда выполняется очень долго

**Это нормально для следующих команд:**
- `apt update && apt upgrade -y` - **2-5 минут**
- `sh get-docker.sh` - **3-5 минут**
- `docker-compose build` - **5-10 минут**
- `certbot --nginx` - **1-2 минуты**

**Что делать:**
- ✅ **Подождите** - процесс идет в фоне
- ✅ **НЕ закрывайте консоль**
- ✅ **НЕ прерывайте процесс** (не нажимайте Ctrl+C)
- ✅ Курсор должен мигать - это значит, что процесс активен

**Если прошло больше 15 минут:**
- Проверьте интернет-соединение
- Попробуйте прервать (`Ctrl + C`) и запустить заново

---

## 💡 Полезные советы для работы с веб-консолью

1. **Копирование команд:**
   - Выделите текст в браузере (мышью)
   - Вставьте в консоль через контекстное меню или `Ctrl + Shift + V`

2. **Прокрутка консоли:**
   - Используйте колесико мыши
   - Или полосу прокрутки справа

3. **Увеличение размера консоли:**
   - В noVNC можно изменить размер окна
   - Или используйте полноэкранный режим (F11)

4. **Работа с редактором nano:**
   - `Ctrl + O` - сохранить
   - `Ctrl + X` - выйти
   - `Ctrl + K` - вырезать строку
   - `Ctrl + U` - вставить

5. **Прерывание выполнения команды:**
   - Нажмите `Ctrl + C`

6. **Очистка экрана:**
   - Введите `clear` или нажмите `Ctrl + L`

---

## ✅ Готово!

После выполнения всех шагов ваш сайт будет доступен по адресу:
- **http://derevo.space** (или **https://derevo.space** после настройки SSL)

**Успешного развертывания! 🎉**

---

## 📞 Если нужна помощь

1. **Проверьте логи приложения:**
   ```bash
   cd /var/www/derevo
   docker-compose logs -f
   ```

2. **Проверьте логи Nginx:**
   ```bash
   tail -f /var/log/nginx/derevo-error.log
   ```

3. **Убедитесь, что все переменные окружения заполнены правильно:**
   ```bash
   cat /var/www/derevo/.env.production
   ```

4. **Проверьте статус всех сервисов:**
   ```bash
   systemctl status docker nginx
   docker-compose ps
   ```

---

## 🎯 Краткая памятка команд

```bash
# Переход в директорию проекта
cd /var/www/derevo

# Просмотр статуса
docker-compose ps

# Просмотр логов
docker-compose logs -f

# Перезапуск
docker-compose restart

# Остановка
docker-compose down

# Запуск
docker-compose up -d

# Проверка Nginx
systemctl status nginx
nginx -t

# Перезапуск Nginx
systemctl restart nginx
```

**Все команды можно выполнять в веб-консоли Proxmox!** 🚀

