# 🔥 Открытие портов в файрволе

## ⚠️ Проблема: "Timeout during connect (likely firewall problem)"

**Ошибка Certbot:**
```
Type: connection
Detail: 193.32.188.93: Fetching http://derevo.space/.well-known/acme-challenge/...: 
Timeout during connect (likely firewall problem)
```

**Причина:** Порт 80 (HTTP) заблокирован файрволом, и Let's Encrypt не может подключиться к серверу.

---

## ✅ Решение: Откройте порты 80 и 443

### Метод 1: Через UFW (Uncomplicated Firewall) - Рекомендуется

**Проверьте, установлен ли UFW:**

```bash
which ufw
```

**Если установлен:**

```bash
# Проверьте статус
ufw status

# Если файрвол не активен, активируйте его
ufw enable

# Откройте порт 80 (HTTP)
ufw allow 80/tcp

# Откройте порт 443 (HTTPS)
ufw allow 443/tcp

# Проверьте статус снова
ufw status
```

**Ожидаемый результат:**
```
Status: active

To                         Action      From
--                         ------      ----
80/tcp                     ALLOW       Anywhere
443/tcp                    ALLOW       Anywhere
80/tcp (v6)                ALLOW       Anywhere (v6)
443/tcp (v6)               ALLOW       Anywhere (v6)
```

**Если UFW не установлен, установите его:**

```bash
apt-get update
apt-get install ufw -y
ufw enable
ufw allow 80/tcp
ufw allow 443/tcp
ufw status
```

---

### Метод 2: Через iptables

**Если UFW не используется, используйте iptables:**

```bash
# Откройте порт 80 (HTTP)
iptables -A INPUT -p tcp --dport 80 -j ACCEPT

# Откройте порт 443 (HTTPS)
iptables -A INPUT -p tcp --dport 443 -j ACCEPT

# Проверьте правила
iptables -L -n | grep -E '80|443'
```

**Сохраните правила (чтобы они не сбросились после перезагрузки):**

**Для Debian/Ubuntu:**

```bash
# Установите iptables-persistent (если еще не установлен)
apt-get install iptables-persistent -y

# Сохраните текущие правила
netfilter-persistent save
```

**Или вручную:**

```bash
# Сохраните правила
iptables-save > /etc/iptables/rules.v4

# Для автоматической загрузки при старте создайте скрипт:
cat > /etc/network/if-pre-up.d/iptables << 'EOF'
#!/bin/sh
/sbin/iptables-restore < /etc/iptables/rules.v4
EOF

chmod +x /etc/network/if-pre-up.d/iptables
```

---

### Метод 3: Через панель управления VDSKA ⚠️ ВАЖНО!

**VDSKA может блокировать порты на уровне провайдера!**

#### Вариант А: Панель управления VDSKA

1. **Войдите в панель управления VDSKA:**
   - Обычно это отдельная панель управления VDS
   - Или используйте Proxmox: https://ru202.vdska.ru:8006

2. **Найдите раздел:**
   - "Firewall" (Файрвол)
   - "Network" (Сеть)
   - "Security" (Безопасность)
   - "Ports" (Порты)

3. **Откройте порты:**
   - Порт `80` (TCP) - для HTTP
   - Порт `443` (TCP) - для HTTPS

4. **Сохраните изменения**

#### Вариант Б: Proxmox VE Firewall

**Если используете Proxmox VE:**

1. **Войдите в Proxmox:**
   - URL: https://ru202.vdska.ru:8006
   - Логин и пароль от Proxmox

2. **Перейдите в:**
   - `Datacenter` → `Firewall` → `Options`

3. **Настройте правила:**
   - Добавьте правило для порта 80 (TCP)
   - Добавьте правило для порта 443 (TCP)

4. **Или через командную строку:**

```bash
# Проверьте статус файрвола Proxmox
pve-firewall status

# Если файрвол активен, отключите его временно для теста
pve-firewall stop

# Или настройте правила через веб-интерфейс
```

---

### Метод 4: Проверка и открытие через netfilter

**Проверьте текущие правила:**

```bash
# Просмотр всех правил
iptables -L -n -v

# Проверка конкретных портов
iptables -L INPUT -n | grep -E '80|443'
```

**Если порты закрыты, откройте их:**

```bash
# Очистите старые правила (ОСТОРОЖНО! Может заблокировать доступ!)
# iptables -F

# Добавьте правила для портов
iptables -I INPUT -p tcp --dport 80 -j ACCEPT
iptables -I INPUT -p tcp --dport 443 -j ACCEPT

# Сохраните
iptables-save > /etc/iptables/rules.v4
```

---

## 🔍 Проверка доступности портов

### Проверка 1: Локальная проверка (на сервере)

```bash
# Проверьте, что порты слушаются
netstat -tlnp | grep -E ':80|:443'
# Или
ss -tlnp | grep -E ':80|:443'
```

**Ожидаемый результат:**
```
tcp  0  0 0.0.0.0:80   0.0.0.0:*  LISTEN  12345/nginx
tcp  0  0 0.0.0.0:443  0.0.0.0:*  LISTEN  12345/nginx
```

**Если порты не слушаются:**

```bash
# Проверьте статус Nginx
systemctl status nginx

# Запустите Nginx, если не запущен
systemctl start nginx
systemctl enable nginx
```

### Проверка 2: Проверка с внешнего IP

**Используйте онлайн-сервисы:**

1. **YouGetSignal:**
   - https://www.yougetsignal.com/tools/open-ports/
   - Введите IP: `193.32.188.93`
   - Проверьте порты: `80` и `443`
   - Нажмите "Check"

2. **PortChecker:**
   - https://portchecker.co/
   - Введите IP и порт

3. **CanYouSeeMe:**
   - https://canyouseeme.org/
   - Введите порт `80` или `443`

**Или с другого сервера:**

```bash
# Проверка порта 80
curl -I http://193.32.188.93

# Проверка порта 443
curl -I https://193.32.188.93
```

**Ожидаемый результат:** Должен вернуться HTTP заголовок (не timeout)

### Проверка 3: Проверка через telnet

```bash
# С другого сервера или компьютера
telnet 193.32.188.93 80
telnet 193.32.188.93 443
```

**Ожидаемый результат:** Должно подключиться (не "Connection refused" или "Connection timed out")

---

## 🛠️ Диагностика проблем

### Проблема 1: Порты открыты локально, но недоступны извне

**Причина:** Файрвол провайдера блокирует порты

**Решение:**
1. Откройте порты в панели управления VDSKA
2. Обратитесь в поддержку VDSKA с запросом открыть порты 80 и 443

### Проблема 2: Nginx не слушает на портах

**Проверка:**

```bash
# Проверьте конфигурацию Nginx
nginx -t

# Проверьте, что Nginx слушает на правильных портах
netstat -tlnp | grep nginx
```

**Решение:**

```bash
# Перезапустите Nginx
systemctl restart nginx

# Проверьте логи
tail -f /var/log/nginx/error.log
```

### Проблема 3: Файрвол блокирует даже после открытия

**Решение:**

```bash
# Временно отключите файрвол для теста (ОСТОРОЖНО!)
ufw disable
# Или
iptables -F

# Попробуйте получить сертификат
certbot --nginx -d derevo.space -d www.derevo.space

# Если работает, значит проблема в файрволе - настройте правила правильно
# Затем включите файрвол обратно
ufw enable
```

---

## ✅ После открытия портов

1. **Проверьте доступность:**
   ```bash
   curl -I http://193.32.188.93
   ```

2. **Попробуйте получить SSL сертификат:**
   ```bash
   certbot --nginx -d derevo.space -d www.derevo.space
   ```

3. **Проверьте работу сайта:**
   - Откройте в браузере: `http://derevo.space`
   - Должен открыться сайт (без HTTPS пока)

---

## 📋 Чек-лист

- [ ] Проверен статус UFW: `ufw status`
- [ ] Открыт порт 80: `ufw allow 80/tcp`
- [ ] Открыт порт 443: `ufw allow 443/tcp`
- [ ] Проверено, что порты слушаются: `netstat -tlnp | grep -E ':80|:443'`
- [ ] Проверена доступность с внешнего IP (онлайн-сервис)
- [ ] Проверена панель управления VDSKA (если есть)
- [ ] Проверен файрвол Proxmox (если используется)
- [ ] Попробован Certbot снова: `certbot --nginx -d derevo.space -d www.derevo.space`

---

## ✅ Готово!

После открытия портов Certbot сможет подключиться к серверу и получить SSL сертификат.

**Удачи! 🚀**

