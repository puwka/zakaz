# 🌐 Обновление DNS записей для домена

## ⚠️ Проблема: Домены указывают на неправильный IP

**Текущая ситуация:**
- `derevo.space` указывает на `95.163.244.138` ❌
- `www.derevo.space` указывает на `95.163.244.138` ❌
- **Нужно:** Оба должны указывать на `193.32.188.93` ✅

**Проверка:**
```bash
nslookup derevo.space
nslookup www.derevo.space
```

---

## ✅ Решение: Обновить DNS записи

### Шаг 1: Войдите в панель управления регистратора домена

**Где искать:**
- Панель управления у регистратора, где был куплен домен `derevo.space`
- Обычно это: Namecheap, GoDaddy, Reg.ru, Timeweb, REGISTRAR.RU и т.д.

**Как найти:**
- Проверьте email, который использовали при регистрации домена
- Или используйте WHOIS для поиска регистратора:
  ```bash
  whois derevo.space
  ```

### Шаг 2: Найдите раздел управления DNS

**Обычные названия разделов:**
- "DNS Management" (Управление DNS)
- "DNS Records" (DNS записи)
- "Zone Editor" (Редактор зоны)
- "Настройки DNS"
- "DNS-зона"

### Шаг 3: Обновите A записи

**Найдите или создайте следующие записи:**

#### Запись 1: Основной домен
- **Тип:** `A` (или `A Record`)
- **Имя/Хост:** `@` или `derevo.space` или оставить пустым
- **Значение/IP:** `193.32.188.93`
- **TTL:** `3600` (или автоматически)

#### Запись 2: Поддомен www
**Вариант А: A запись**
- **Тип:** `A` (или `A Record`)
- **Имя/Хост:** `www`
- **Значение/IP:** `193.32.188.93`
- **TTL:** `3600` (или автоматически)

**Вариант Б: CNAME запись (рекомендуется)**
- **Тип:** `CNAME` (или `CNAME Record`)
- **Имя/Хост:** `www`
- **Значение:** `derevo.space` (или `@`)
- **TTL:** `3600` (или автоматически)

### Шаг 4: Сохраните изменения

**После обновления:**
- Нажмите "Сохранить" или "Применить изменения"
- Подождите подтверждения

### Шаг 5: Подождите распространения DNS

**Время распространения:**
- Обычно: **5-15 минут**
- Максимум: **24 часа** (но обычно быстрее)

**Проверка распространения:**

```bash
# Проверка с сервера
nslookup derevo.space
nslookup www.derevo.space

# Или с вашего компьютера
nslookup derevo.space 8.8.8.8
nslookup www.derevo.space 8.8.8.8
```

**Ожидаемый результат:** Оба должны вернуть IP `193.32.188.93`

### Шаг 6: После обновления DNS

**Проверьте доступность сайта:**

```bash
# С сервера
curl -I http://derevo.space
curl -I http://www.derevo.space
```

**Попробуйте получить SSL сертификат:**

```bash
certbot --nginx -d derevo.space -d www.derevo.space
```

---

## 📋 Примеры для разных регистраторов

### Namecheap

1. Войдите в аккаунт
2. Перейдите в "Domain List"
3. Нажмите "Manage" рядом с доменом
4. Перейдите в "Advanced DNS"
5. Найдите A записи
6. Измените IP на `193.32.188.93`
7. Сохраните

### GoDaddy

1. Войдите в аккаунт
2. Перейдите в "My Products"
3. Найдите домен → "DNS"
4. Найдите A записи
5. Измените IP на `193.32.188.93`
6. Сохраните

### REG.RU / REGISTRAR.RU

1. Войдите в личный кабинет
2. Перейдите в "Домены"
3. Найдите домен → "Управление DNS"
4. Найдите A записи
5. Измените IP на `193.32.188.93`
6. Сохраните

### Timeweb

1. Войдите в панель управления
2. Перейдите в "Домены"
3. Найдите домен → "DNS-зона"
4. Найдите A записи
5. Измените IP на `193.32.188.93`
6. Сохраните

---

## 🔍 Проверка DNS записей

### С сервера:

```bash
nslookup derevo.space
nslookup www.derevo.space
```

### С вашего компьютера:

**Windows (PowerShell):**
```powershell
nslookup derevo.space
nslookup www.derevo.space
```

**Или через онлайн-сервисы:**
- https://dnschecker.org/
- https://www.whatsmydns.net/
- Введите домен и проверьте IP адрес

---

## ❗ Важные замечания

1. **Не удаляйте другие DNS записи** (MX, TXT и т.д.), если они нужны
2. **Обновляйте только A записи** для `@` и `www`
3. **Подождите распространения DNS** перед попыткой получить SSL сертификат
4. **Проверяйте DNS с разных серверов** для уверенности

---

## ✅ После обновления DNS

1. **Проверьте DNS:**
   ```bash
   nslookup derevo.space
   nslookup www.derevo.space
   ```

2. **Проверьте доступность сайта:**
   ```bash
   curl -I http://derevo.space
   ```

3. **Получите SSL сертификат:**
   ```bash
   certbot --nginx -d derevo.space -d www.derevo.space
   ```

---

## 📞 Если не можете найти панель управления

1. **Используйте WHOIS:**
   ```bash
   whois derevo.space
   ```
   Найдите информацию о регистраторе

2. **Обратитесь в поддержку регистратора:**
   - Сообщите, что нужно обновить DNS записи
   - Укажите домен: `derevo.space`
   - Укажите новый IP: `193.32.188.93`

---

## ✅ Готово!

После обновления DNS записей и их распространения:
- Домены будут указывать на правильный IP
- Certbot сможет получить SSL сертификат
- Сайт будет доступен по HTTPS

**Удачи! 🚀**


