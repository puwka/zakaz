# 🔧 Решение проблем с сайтом

## ❌ Проблема: Сайт не загружается / Ошибки в логах

**Симптомы:**
- Ошибка: `'sharp' is required to be installed in standalone mode`
- Ошибки Server Actions: `Failed to find Server Action`
- Ошибка сборки: `npm error Missing: @img/sharp-linux-x64@1.0.4` ⚠️ **НОВАЯ ПРОБЛЕМА!**
- Сайт не открывается или показывает ошибки

---

## ✅ Решение: Обновить зависимости и пересобрать

### Шаг 1: Обновите код на сервере

**Если используете Git:**

```bash
cd /var/www/derevo
git pull origin main
```

**Или обновите файлы вручную:**
- `package.json` - должен содержать `"sharp": "^0.33.0"`
- `Dockerfile` - должен использовать `node:20-slim` и `npm install` вместо `npm ci`

**⚠️ ВАЖНО:** Если видите ошибки `npm error Missing:`, это значит `package-lock.json` не синхронизирован. Dockerfile уже обновлен для использования `npm install`, который автоматически обновит lock file.

### Шаг 2: Пересоберите Docker образ

```bash
cd /var/www/derevo

# Остановите текущие контейнеры
docker-compose down

# Пересоберите образ с новыми зависимостями
docker-compose build --no-cache

# Запустите контейнеры
docker-compose up -d

# Проверьте логи
docker-compose logs -f
```

**Ожидаемый результат:** 
- Нет ошибок про `sharp`
- Нет ошибок Server Actions
- Приложение запускается успешно

### Шаг 3: Проверьте работу сайта

```bash
# Проверьте статус контейнеров
docker-compose ps

# Проверьте, что приложение отвечает
curl http://localhost:3000

# Проверьте логи на ошибки
docker-compose logs | grep -i error
```

**Если есть ошибки, проверьте:**
- Файл `.env.production` заполнен правильно
- Все переменные окружения установлены

---

## 🔍 Диагностика

### Проверка 1: Статус контейнеров

```bash
docker-compose ps
```

**Ожидаемый результат:**
```
NAME                STATUS              PORTS
derevo-app-1        Up X minutes        0.0.0.0:3000->3000/tcp
```

### Проверка 2: Логи приложения

```bash
docker-compose logs app
```

**Ищите ошибки:**
- ❌ `'sharp' is required` - нужно добавить sharp в package.json
- ❌ `Failed to find Server Action` - может быть временная проблема, попробуйте перезапустить
- ❌ `Cannot connect to database` - проверьте переменные окружения

### Проверка 3: Доступность приложения

```bash
# Локально
curl http://localhost:3000

# Через Nginx
curl http://localhost
```

**Ожидаемый результат:** HTML код страницы

### Проверка 4: Переменные окружения

```bash
# Проверьте, что файл существует
ls -la .env.production

# Проверьте содержимое (без показа значений)
cat .env.production | grep -E '^[A-Z_]+=' | cut -d'=' -f1
```

**Должны быть установлены:**
- `NEXT_PUBLIC_SUPABASE_URL`
- `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- `SUPABASE_SERVICE_ROLE_KEY`
- `TELEGRAM_BOT_TOKEN`
- `TELEGRAM_CHAT_ID`

---

## 🛠️ Дополнительные исправления

### Если проблема с Server Actions

**Это может быть временная проблема Next.js. Попробуйте:**

```bash
# Перезапустите контейнеры
docker-compose restart

# Или полностью пересоберите
docker-compose down
docker-compose build --no-cache
docker-compose up -d
```

### Если проблема с sharp

**Убедитесь, что sharp добавлен в package.json:**

```bash
# Проверьте package.json
grep sharp package.json

# Должно быть:
# "sharp": "^0.33.0"
```

**Если нет, добавьте вручную:**

```bash
# Войдите в контейнер
docker-compose exec app sh

# Установите sharp
npm install sharp

# Выйдите
exit

# Перезапустите
docker-compose restart
```

**Или обновите package.json и пересоберите образ.**

### Если проблема с базой данных

**Проверьте подключение к Supabase:**

```bash
# Проверьте переменные окружения
docker-compose exec app env | grep SUPABASE

# Проверьте доступность Supabase
curl https://amidqohwruemmvigubrm.supabase.co
```

---

## 📋 Чек-лист исправления

- [ ] Обновлен `package.json` (добавлен `sharp`)
- [ ] Обновлен `Dockerfile` (использует `node:20-slim`)
- [ ] Код обновлен на сервере (`git pull` или вручную)
- [ ] Docker образ пересобран (`docker-compose build --no-cache`)
- [ ] Контейнеры перезапущены (`docker-compose up -d`)
- [ ] Проверены логи (`docker-compose logs`)
- [ ] Проверена доступность (`curl http://localhost:3000`)
- [ ] Проверены переменные окружения (`.env.production`)

---

## ✅ После исправления

1. **Проверьте сайт в браузере:**
   - `http://derevo.space` (или IP адрес)
   - Должен открыться сайт без ошибок

2. **Проверьте админ-панель:**
   - `http://derevo.space/admin/login`
   - Должна открыться страница входа

3. **Проверьте логи на ошибки:**
   ```bash
   docker-compose logs -f
   ```
   - Не должно быть критических ошибок

---

## 📞 Если ничего не помогает

1. **Проверьте все логи:**
   ```bash
   docker-compose logs > logs.txt
   cat logs.txt
   ```

2. **Проверьте конфигурацию:**
   ```bash
   docker-compose config
   ```

3. **Попробуйте запустить в интерактивном режиме:**
   ```bash
   docker-compose down
   docker-compose up
   ```
   (без `-d` для просмотра логов в реальном времени)

4. **Проверьте системные ресурсы:**
   ```bash
   free -h
   df -h
   ```

---

## ✅ Готово!

После исправления всех проблем сайт должен работать корректно.

**Удачи! 🚀**

