# Инструкция по развертыванию на VDS

## 📋 Требования

- VDS сервер с Ubuntu 20.04+ или Debian 11+
- SSH доступ к серверу
- Домен `derevo.space` настроен на IP сервера

## 🔧 Шаг 1: Подключение к серверу

```bash
ssh root@193.32.188.93
# Пароль: Zt7Ncu36feJh
```

## 📦 Шаг 2: Установка необходимого ПО

### Автоматическая установка (рекомендуется)
```bash
# Загрузите setup-server.sh на сервер и выполните:
bash setup-server.sh
```

### Ручная установка

#### Обновление системы
```bash
apt update && apt upgrade -y
```

#### Установка Docker и Docker Compose
```bash
# Установка Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh

# Установка Docker Compose
apt install docker-compose -y

# Проверка установки
docker --version
docker-compose --version
```

#### Установка Nginx
```bash
apt install nginx -y
systemctl enable nginx
systemctl start nginx
```

#### Установка Node.js (для сборки, если нужно)
```bash
curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
apt install -y nodejs
```

## 📁 Шаг 3: Подготовка проекта на сервере

### Создание директории для проекта
```bash
mkdir -p /var/www/derevo
cd /var/www/derevo
```

### Загрузка проекта

**Вариант 1: Через Git (рекомендуется)**
```bash
# Если проект в Git репозитории
git clone <ваш-репозиторий> .

# Или создайте репозиторий и загрузите файлы
```

**Вариант 2: Через SCP (с локального компьютера)**
```bash
# На вашем локальном компьютере выполните:
scp -r . root@193.32.188.93:/var/www/derevo/
```

**Вариант 3: Через rsync (с локального компьютера)**
```bash
# На вашем локальном компьютере выполните:
rsync -avz --exclude 'node_modules' --exclude '.next' \
  ./ root@193.32.188.93:/var/www/derevo/
```

## 🔐 Шаг 4: Настройка переменных окружения

Создайте файл `.env.production` на сервере:

```bash
cd /var/www/derevo
nano .env.production
```

Добавьте следующие переменные:

```env
NEXT_PUBLIC_SUPABASE_URL=https://ваш-проект.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш-anon-key
SUPABASE_SERVICE_ROLE_KEY=ваш-service-role-key
TELEGRAM_BOT_TOKEN=ваш-telegram-bot-token
TELEGRAM_CHAT_ID=ваш-telegram-chat-id
```

**Важно:** Замените значения на реальные из вашего Supabase проекта.

Сохраните файл (Ctrl+O, Enter, Ctrl+X).

## 🐳 Шаг 5: Настройка Next.js для standalone режима

Убедитесь, что в `next.config.js` включен standalone режим:

```javascript
/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'standalone', // Добавьте эту строку
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: '**.supabase.co',
      },
      {
        protocol: 'https',
        hostname: 'images.unsplash.com',
      },
    ],
  },
};

module.exports = nextConfig;
```

## 🚀 Шаг 6: Сборка и запуск через Docker

```bash
cd /var/www/derevo

# Сделайте скрипт деплоя исполняемым
chmod +x deploy.sh

# Запустите деплой
./deploy.sh
```

Или вручную:

```bash
# Сборка образа
docker-compose build

# Запуск контейнера
docker-compose up -d

# Просмотр логов
docker-compose logs -f
```

## 🌐 Шаг 7: Настройка Nginx

### Копирование конфигурации
```bash
cp /var/www/derevo/nginx.conf /etc/nginx/sites-available/derevo
ln -s /etc/nginx/sites-available/derevo /etc/nginx/sites-enabled/
```

### Удаление дефолтной конфигурации (опционально)
```bash
rm /etc/nginx/sites-enabled/default
```

### Проверка конфигурации
```bash
nginx -t
```

### Перезапуск Nginx
```bash
systemctl restart nginx
```

## 🔒 Шаг 8: Настройка SSL (Let's Encrypt)

### Установка Certbot
```bash
apt install certbot python3-certbot-nginx -y
```

### Получение SSL сертификата
```bash
certbot --nginx -d derevo.space -d www.derevo.space
```

Следуйте инструкциям на экране. Certbot автоматически обновит конфигурацию Nginx.

### Автоматическое обновление сертификата
```bash
certbot renew --dry-run
```

## ✅ Шаг 9: Проверка работы

1. Откройте в браузере: `http://derevo.space` (или `https://derevo.space` после настройки SSL)
2. Проверьте логи приложения:
   ```bash
   docker-compose logs -f
   ```
3. Проверьте логи Nginx:
   ```bash
   tail -f /var/log/nginx/derevo-access.log
   tail -f /var/log/nginx/derevo-error.log
   ```

## 🔄 Обновление приложения

### Метод 1: Через Git
```bash
cd /var/www/derevo
git pull
./deploy.sh
```

### Метод 2: Вручную
```bash
cd /var/www/derevo
docker-compose down
docker-compose build --no-cache
docker-compose up -d
```

## 📊 Мониторинг и управление

### Просмотр статуса контейнеров
```bash
docker-compose ps
```

### Просмотр логов
```bash
# Все логи
docker-compose logs -f

# Логи только приложения
docker-compose logs -f app
```

### Перезапуск приложения
```bash
docker-compose restart
```

### Остановка приложения
```bash
docker-compose down
```

### Очистка старых образов
```bash
docker system prune -a
```

## 🛠️ Решение проблем

### Приложение не запускается
```bash
# Проверьте логи
docker-compose logs

# Проверьте переменные окружения
cat .env.production

# Проверьте, что порт 3000 свободен
netstat -tulpn | grep 3000
```

### Nginx не работает
```bash
# Проверьте конфигурацию
nginx -t

# Проверьте статус
systemctl status nginx

# Перезапустите
systemctl restart nginx
```

### Проблемы с правами доступа
```bash
# Установите правильные права
chown -R root:root /var/www/derevo
chmod -R 755 /var/www/derevo
```

### Проблемы с Docker
```bash
# Перезапустите Docker
systemctl restart docker

# Проверьте статус
systemctl status docker
```

## 🔐 Безопасность

### Настройка файрвола (UFW)
```bash
# Установка
apt install ufw -y

# Разрешить SSH
ufw allow 22/tcp

# Разрешить HTTP и HTTPS
ufw allow 80/tcp
ufw allow 443/tcp

# Включить файрвол
ufw enable

# Проверить статус
ufw status
```

### Регулярные обновления
```bash
# Добавьте в crontab для автоматических обновлений
crontab -e

# Добавьте строку (обновление каждую неделю):
0 3 * * 0 apt update && apt upgrade -y
```

## 📝 Дополнительные настройки

### Автозапуск при перезагрузке сервера
Docker Compose автоматически запускает контейнеры при перезагрузке благодаря `restart: unless-stopped` в `docker-compose.yml`.

### Резервное копирование
Рекомендуется настроить регулярное резервное копирование:
- База данных Supabase (через Supabase Dashboard)
- Файлы проекта (если есть локальные файлы)
- Конфигурационные файлы

## 📞 Контакты и поддержка

Если возникнут проблемы:
1. Проверьте логи: `docker-compose logs -f`
2. Проверьте статус сервисов: `systemctl status nginx docker`
3. Проверьте конфигурацию: `nginx -t`

---

**Успешного деплоя! 🚀**
