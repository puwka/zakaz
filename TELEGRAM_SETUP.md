# Настройка отправки заявок в Telegram

## Инструкция по настройке

### 1. Создание Telegram бота

1. Откройте Telegram и найдите бота [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Придумайте имя для бота (например: "Заявки с сайта")
   - Придумайте username для бота (должен заканчиваться на `bot`, например: `my_website_bot`)
4. BotFather выдаст вам **токен бота** (выглядит примерно так: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)
5. **Сохраните этот токен** - он понадобится для настройки

### 2. Получение Chat ID

Есть несколько способов получить Chat ID:

#### Способ 1: Через бота @userinfobot
1. Найдите бота [@userinfobot](https://t.me/userinfobot) в Telegram
2. Начните диалог с ботом
3. Бот отправит вам ваш Chat ID (число, например: `123456789`)

#### Способ 2: Через бота @getidsbot
1. Найдите бота [@getidsbot](https://t.me/getidsbot) в Telegram
2. Добавьте бота в группу или начните диалог
3. Бот покажет Chat ID

#### Способ 3: Создать группу и добавить бота
1. Создайте новую группу в Telegram
2. Добавьте вашего бота в группу
3. Отправьте любое сообщение в группу
4. Откройте в браузере: `https://api.telegram.org/bot<ВАШ_ТОКЕН>/getUpdates`
5. Найдите в ответе `"chat":{"id":-123456789}` - это и есть Chat ID группы (для группы будет отрицательное число)

### 3. Настройка переменных окружения

1. Создайте файл `.env.local` в корне проекта (если его еще нет)
2. Добавьте следующие переменные:

```env
TELEGRAM_BOT_TOKEN=ваш_токен_бота
TELEGRAM_CHAT_ID=ваш_chat_id
```

**Пример:**
```env
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_CHAT_ID=123456789
```

### 4. Перезапуск сервера

После добавления переменных окружения:
1. Остановите сервер разработки (Ctrl+C)
2. Запустите снова: `npm run dev`
3. Переменные окружения загрузятся автоматически

### 5. Тестирование

1. Откройте страницу "Контакты" на вашем сайте
2. Заполните форму обратной связи
3. Отправьте заявку
4. Проверьте Telegram - должно прийти сообщение с данными заявки

### Формат сообщения

Сообщения будут приходить в следующем формате:

```
🔔 Новая заявка с сайта

👤 Имя: Иван Иванов
📞 Телефон: +7 (999) 123-45-67
💬 Сообщение:
Расскажите о вашем проекте...

⏰ Время: 15 января 2024 г., 14:30
```

### Устранение проблем

**Сообщения не приходят:**
1. Проверьте, что переменные окружения правильно указаны в `.env.local`
2. Убедитесь, что токен бота правильный (без пробелов)
3. Проверьте, что Chat ID правильный (может быть отрицательным для групп)
4. Убедитесь, что бот добавлен в группу (если используете группу)
5. Проверьте консоль сервера на наличие ошибок

**Ошибка "Telegram не настроен на сервере":**
- Убедитесь, что файл `.env.local` существует и содержит обе переменные
- Перезапустите сервер разработки

**Ошибка "Ошибка при отправке сообщения в Telegram":**
- Проверьте токен бота - возможно, он неверный
- Проверьте Chat ID - возможно, бот не добавлен в группу или чат не существует
- Убедитесь, что бот активен (не заблокирован)

### Безопасность

⚠️ **ВАЖНО:** 
- Никогда не коммитьте файл `.env.local` в Git
- Файл `.env.local` уже должен быть в `.gitignore`
- Не делитесь токеном бота и Chat ID публично

### Для продакшена

При деплое на Vercel или другой хостинг:
1. Добавьте переменные окружения в настройках проекта
2. Убедитесь, что переменные называются точно так же: `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID`
3. После добавления переменных перезапустите деплой

