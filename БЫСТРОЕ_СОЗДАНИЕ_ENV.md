# ⚡ Быстрое создание .env.production без nano

## 🎯 Если вставка в nano не работает, используйте эти способы:

---

## ✅ Способ 1: Через cat (САМЫЙ ПРОСТОЙ) ⭐

**Скопируйте и выполните эту команду целиком:**

```bash
cat > /var/www/derevo/.env.production << 'EOF'
NEXT_PUBLIC_SUPABASE_URL=https://ваш-проект.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш-anon-key
SUPABASE_SERVICE_ROLE_KEY=ваш-service-role-key
TELEGRAM_BOT_TOKEN=ваш-telegram-bot-token
TELEGRAM_CHAT_ID=ваш-telegram-chat-id
EOF
```

**Как использовать:**
1. **Замените** `ваш-проект.supabase.co`, `ваш-anon-key` и т.д. на реальные значения
2. **Скопируйте** всю команду (от `cat` до `EOF`)
3. **Вставьте** в веб-консоль (правый клик → Paste)
4. **Нажмите Enter**
5. Файл создан!

**Проверка:**
```bash
cat /var/www/derevo/.env.production
```

---

## ✅ Способ 2: Построчно через echo

**Выполните команды по порядку:**

```bash
cd /var/www/derevo
```

```bash
echo "NEXT_PUBLIC_SUPABASE_URL=https://ваш-проект.supabase.co" > .env.production
```

```bash
echo "NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш-anon-key" >> .env.production
```

```bash
echo "SUPABASE_SERVICE_ROLE_KEY=ваш-service-role-key" >> .env.production
```

```bash
echo "TELEGRAM_BOT_TOKEN=ваш-telegram-bot-token" >> .env.production
```

```bash
echo "TELEGRAM_CHAT_ID=ваш-telegram-chat-id" >> .env.production
```

**Важно:**
- Первая команда использует `>` (создает файл)
- Остальные команды используют `>>` (добавляют в конец)

**Проверка:**
```bash
cat .env.production
```

---

## ✅ Способ 3: Редактирование существующего файла через sed

**Если файл уже создан, можно редактировать через sed:**

```bash
cd /var/www/derevo
```

**Заменить значение:**
```bash
sed -i 's|ваш-проект.supabase.co|реальный-url.supabase.co|g' .env.production
```

**Добавить новую строку в конец:**
```bash
echo "НОВАЯ_ПЕРЕМЕННАЯ=значение" >> .env.production
```

---

## ✅ Способ 4: Загрузка файла с компьютера

**Если ничего не работает, загрузите файл с вашего компьютера:**

### Через WinSCP:

1. **Создайте файл на вашем компьютере:**
   - Откройте Блокнот или любой текстовый редактор
   - Создайте файл с именем `.env.production`
   - Заполните его:
     ```
     NEXT_PUBLIC_SUPABASE_URL=https://ваш-проект.supabase.co
     NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш-anon-key
     SUPABASE_SERVICE_ROLE_KEY=ваш-service-role-key
     TELEGRAM_BOT_TOKEN=ваш-telegram-bot-token
     TELEGRAM_CHAT_ID=ваш-telegram-chat-id
     ```
   - Сохраните файл

2. **Подключитесь через WinSCP:**
   - Хост: `193.32.188.93`
   - Пользователь: `root`
   - Пароль: `Zt7Ncu36feJh`
   - Протокол: `SFTP`

3. **Загрузите файл:**
   - Перейдите в `/var/www/derevo`
   - Перетащите файл `.env.production` в эту папку

### Через SCP (из терминала на вашем компьютере):

```bash
# На вашем компьютере (PowerShell или Git Bash)
scp .env.production root@193.32.188.93:/var/www/derevo/
```

---

## 📝 Пример с реальными значениями

**Замените значения на ваши реальные:**

```bash
cat > /var/www/derevo/.env.production << 'EOF'
NEXT_PUBLIC_SUPABASE_URL=https://abcdefghijklmnop.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImFiY2RlZmdoaWprbG1ub3AiLCJyb2xlIjoiYW5vbiIsImlhdCI6MTYxNjIzOTAyMiwiZXhwIjoxOTMxODE1MDIyfQ.пример
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImFiY2RlZmdoaWprbG1ub3AiLCJyb2xlIjoic2VydmljZV9yb2xlIiwiaWF0IjoxNjE2MjM5MDIyLCJleHAiOjE5MzE4MTUwMjJ9.пример
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_CHAT_ID=123456789
EOF
```

---

## ✅ Проверка после создания

```bash
# Перейдите в директорию
cd /var/www/derevo

# Просмотрите содержимое файла
cat .env.production

# Проверьте, что файл существует
ls -la .env.production
```

**Должны быть видны ваши переменные окружения.**

---

## 🔧 Редактирование файла после создания

**Если нужно изменить значение:**

```bash
# Откройте файл в nano
nano /var/www/derevo/.env.production

# Или используйте sed для замены
sed -i 's|старое-значение|новое-значение|g' /var/www/derevo/.env.production
```

---

## 💡 Полезные советы

1. **Используйте Способ 1 (cat)** - это самый простой и надежный способ
2. **Не забывайте заменять** `ваш-проект.supabase.co` на реальные значения
3. **Проверяйте файл** после создания командой `cat .env.production`
4. **Если файл уже существует**, он будет перезаписан (старое содержимое удалится)

---

## ✅ Готово!

После создания файла `.env.production` продолжайте с инструкции:
- Шаг 5: Запуск приложения
- Или следующий шаг из **ИНСТРУКЦИЯ_ВЕБ_КОНСОЛЬ.md**

**Удачи! 🚀**


