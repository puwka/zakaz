# 🔧 Решение проблемы с интернет-соединением

## ❌ Ошибка: Failed to connect to get.docker.com

Если вы видите ошибку:
```
curl: (28) Failed to connect to get.docker.com port 443 after 300595 ms: Timeout was reached
```

Это означает, что сервер не может подключиться к интернету или есть проблемы с DNS.

---

## 🔍 Диагностика

### 1. Проверьте интернет-соединение

```bash
ping -c 4 8.8.8.8
```

**Ожидаемый результат:** Должны быть ответы от 8.8.8.8 (Google DNS)

**Если не работает:**
- Проблема с интернет-соединением на сервере
- Проверьте настройки сети в Proxmox

### 2. Проверьте DNS

```bash
nslookup get.docker.com
```

**Ожидаемый результат:** Должен вернуться IP адрес get.docker.com

**Если не работает:**
- Проблема с DNS
- Попробуйте изменить DNS серверы

### 3. Проверьте доступность сайта

```bash
curl -I https://www.google.com
```

**Ожидаемый результат:** Должен вернуться HTTP заголовок

**Если не работает:**
- Проблема с HTTPS соединениями
- Возможно, файрвол блокирует исходящие соединения

---

## ✅ Решения

### Решение 1: Установка Docker через apt (РЕКОМЕНДУЕТСЯ)

Если нет интернета или проблемы с подключением к get.docker.com, установите Docker из репозитория Debian:

```bash
# Обновим список пакетов
apt update
```

```bash
# Установим Docker из репозитория Debian
apt install -y docker.io docker-compose
```

**Это установит Docker без необходимости подключаться к get.docker.com**

**Проверка установки:**

```bash
docker --version
docker-compose --version
```

**Ожидаемый результат:** Должны показаться версии Docker и Docker Compose

### Решение 2: Изменение DNS серверов

Если проблема с DNS, измените DNS серверы:

```bash
# Отредактируйте файл с DNS
nano /etc/resolv.conf
```

**Добавьте или замените содержимое на:**
```
nameserver 8.8.8.8
nameserver 8.8.4.4
```

**Сохраните:** `Ctrl + O`, `Enter`, `Ctrl + X`

**Проверьте:**

```bash
nslookup get.docker.com
```

Теперь должно работать.

### Решение 3: Проверка файрвола

Проверьте, не блокирует ли файрвол исходящие соединения:

```bash
ufw status
```

**Если файрвол активен:**
- Возможно, он блокирует исходящие соединения
- Попробуйте временно отключить для проверки:

```bash
ufw disable
```

**Попробуйте снова:**

```bash
curl -fsSL https://get.docker.com -o get-docker.sh
```

**Если работает - включите файрвол обратно:**

```bash
ufw enable
```

### Решение 4: Проверка настроек сети в Proxmox

1. **Войдите в Proxmox:** https://ru202.vdska.ru:8006
2. **Найдите вашу VM**
3. **Перейдите в раздел "Hardware" → "Network"**
4. **Проверьте настройки:**
   - Bridge должен быть выбран правильно
   - Интерфейс должен быть включен
   - IP адрес должен быть назначен

5. **Если нужно, перезапустите сетевой интерфейс:**

```bash
# В веб-консоли VM
systemctl restart networking
```

### Решение 5: Использование прокси (если есть)

Если у вас есть прокси-сервер, настройте его:

```bash
# Установите переменные окружения для прокси
export http_proxy=http://прокси:порт
export https_proxy=http://прокси:порт
```

**Попробуйте снова:**

```bash
curl -fsSL https://get.docker.com -o get-docker.sh
```

---

## 🎯 Рекомендуемый порядок действий

1. **Попробуйте Решение 1** (установка через apt) - это самый простой способ
2. Если не помогло - **проверьте интернет** (Решение 1, диагностика)
3. Если интернет есть, но DNS не работает - **Решение 2** (изменение DNS)
4. Если ничего не помогает - **обратитесь к провайдеру** VDS

---

## ✅ После решения проблемы

После того, как Docker установлен (любым способом), продолжайте с инструкции:

1. Проверьте установку:
   ```bash
   docker --version
   docker-compose --version
   ```

2. Запустите Docker:
   ```bash
   systemctl start docker
   systemctl enable docker
   ```

3. Проверьте, что Docker работает:
   ```bash
   docker ps
   ```

4. Продолжайте с **Шага 2.3** инструкции (установка Docker Compose, если еще не установлен)

---

## 💡 Полезные команды для диагностики

```bash
# Проверка интернета
ping -c 4 8.8.8.8

# Проверка DNS
nslookup get.docker.com
nslookup google.com

# Проверка HTTPS соединений
curl -I https://www.google.com

# Проверка сетевых интерфейсов
ip addr show

# Проверка маршрутизации
ip route show

# Проверка файрвола
ufw status
iptables -L

# Проверка DNS конфигурации
cat /etc/resolv.conf
```

---

## 📞 Если ничего не помогает

1. **Проверьте логи:**
   ```bash
   journalctl -xe
   ```

2. **Обратитесь к провайдеру VDS:**
   - Сообщите о проблеме с интернет-соединением
   - Попросите проверить настройки сети
   - Уточните, не блокируются ли исходящие соединения

3. **Используйте альтернативный способ установки:**
   - Установите Docker через apt (Решение 1)
   - Это не требует подключения к get.docker.com

---

## ✅ Готово!

После установки Docker любым способом, продолжайте с инструкции **ИНСТРУКЦИЯ_ВЕБ_КОНСОЛЬ.md** с шага 2.3.


