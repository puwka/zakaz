# 📘 Инструкция по установке сайта на сервер

## 🎯 Что нужно сделать

Развернуть сайт на вашем VDS сервере по адресу `derevo.space`

> ⚠️ **Важно:** Ваш сервер работает на **Proxmox VE**.  
> Если у вас проблемы с подключением через SSH, используйте **веб-консоль Proxmox**.  
> 📖 **Подробная инструкция:** см. файл [`PROXMOX_CONNECTION.md`](./PROXMOX_CONNECTION.md)  
> 🖥️ **Полная инструкция через веб-консоль:** см. файл [`ИНСТРУКЦИЯ_ВЕБ_КОНСОЛЬ.md`](./ИНСТРУКЦИЯ_ВЕБ_КОНСОЛЬ.md) - **РЕКОМЕНДУЕТСЯ!**

---

## 📝 Шаг 1: Подключение к серверу

### 🖥️ Ваш сервер работает на Proxmox VE

Ваш VDS работает на платформе **Proxmox VE**. Для подключения есть два способа:

---

### 🔐 Способ 1: Через веб-консоль Proxmox (РЕКОМЕНДУЕТСЯ)

Это самый простой способ, если SSH недоступен:

1. **Откройте веб-интерфейс Proxmox:**
   - Перейдите по адресу: **https://ru202.vdska.ru:8006**
   - Войдите с вашими учетными данными Proxmox

2. **Найдите вашу виртуальную машину:**
   - В левой панели найдите вашу VM (виртуальную машину)
   - Убедитесь, что она **запущена** (статус должен быть "Running")

3. **Откройте консоль VM:**
   - Кликните на вашу VM
   - Перейдите в раздел **"Console"** (Консоль)
   - Выберите **"noVNC"** или **"xterm.js"**
   - Откроется веб-консоль, где вы сможете работать с сервером

4. **Войдите в систему:**
   - В консоли введите: `root`
   - Введите пароль: `Zt7Ncu36feJh`

**Документация Proxmox:** https://ru202.vdska.ru:8006/pve-docs/index.html

---

### 🔌 Способ 2: Прямое подключение через SSH

Если SSH порт открыт и доступен:

**На Windows (PowerShell или Git Bash):**
```bash
ssh root@193.32.188.93
```

Когда попросит пароль, введите: `Zt7Ncu36feJh`

**Проверка доступности SSH:**
```powershell
# В PowerShell
Test-NetConnection -ComputerName 193.32.188.93 -Port 22
```

---

### ⚙️ Настройка SSH через Proxmox (если SSH недоступен)

Если SSH не работает, настройте его через веб-консоль Proxmox:

1. **Подключитесь через веб-консоль** (см. Способ 1)

2. **Проверьте, запущен ли SSH:**
   ```bash
   systemctl status ssh
   # или
   systemctl status sshd
   ```

3. **Если SSH не запущен, запустите его:**
   ```bash
   systemctl start ssh
   systemctl enable ssh
   ```

4. **Проверьте файрвол:**
   ```bash
   ufw status
   # Если файрвол активен, откройте порт 22:
   ufw allow 22/tcp
   ```

5. **Проверьте настройки SSH:**
   ```bash
   # Убедитесь, что SSH разрешен в конфигурации
   cat /etc/ssh/sshd_config | grep -E "PermitRootLogin|PasswordAuthentication"
   ```

6. **Перезапустите SSH:**
   ```bash
   systemctl restart ssh
   ```

---

### 🔧 Альтернативные способы подключения

**Через PuTTY (Windows):**
- Скачайте PuTTY: https://www.putty.org/
- Host Name: `193.32.188.93`
- Port: `22`
- Connection type: `SSH`
- Нажмите "Open"
- Логин: `root`, пароль: `Zt7Ncu36feJh`

**Через WinSCP (для загрузки файлов):**
- Скачайте WinSCP: https://winscp.net/
- Хост: `193.32.188.93`
- Пользователь: `root`
- Пароль: `Zt7Ncu36feJh`
- Протокол: `SFTP` или `SCP`

---

### ❗ Решение проблем

**Если SSH недоступен:**
- Используйте **веб-консоль Proxmox** (Способ 1) - это самый надежный вариант
- Через веб-консоль вы можете выполнить все команды установки
- 📖 **Подробное решение проблем с SSH:** см. файл [`SSH_TROUBLESHOOTING.md`](./SSH_TROUBLESHOOTING.md)

**Если не можете войти в Proxmox:**
- Уточните учетные данные для входа в Proxmox у провайдера
- Проверьте, что используете правильный адрес: `https://ru202.vdska.ru:8006`

**Если SSH не работает, но сервер доступен:**
- Скорее всего, файрвол Proxmox блокирует подключения
- Войдите в Proxmox → найдите вашу VM → раздел "Firewall" → добавьте правило для порта 22
- Или просто используйте веб-консоль Proxmox - она работает всегда!

**Если VM не запущена:**
- В веб-интерфейсе Proxmox найдите вашу VM
- Нажмите кнопку **"Start"** (Запустить)
- Дождитесь запуска VM

---

## 🔧 Шаг 2: Установка необходимого ПО

После подключения к серверу выполните команды по порядку:

### 2.1. Обновление системы
```bash
apt update && apt upgrade -y
```

### 2.2. Установка Docker
```bash
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh
rm get-docker.sh
```

### 2.3. Установка Docker Compose
```bash
apt install docker-compose -y
```

### 2.4. Установка Nginx
```bash
apt install nginx -y
systemctl enable nginx
systemctl start nginx
```

### 2.5. Проверка установки
```bash
docker --version
docker-compose --version
nginx -v
```

Все команды должны выполниться без ошибок.

---

## 📦 Шаг 3: Загрузка проекта на сервер

### 3.1. Создайте директорию для проекта
На сервере выполните:
```bash
mkdir -p /var/www/derevo
cd /var/www/derevo
```

### 3.2. Загрузите файлы проекта

**Вариант А: Если у вас есть Git репозиторий**
```bash
# На сервере:
git clone <ваш-репозиторий> /var/www/derevo
cd /var/www/derevo
```

**Вариант Б: Загрузка через SCP (с вашего компьютера)**

Откройте **новый терминал** на вашем компьютере (не закрывая SSH сессию) и выполните:

```bash
# Перейдите в папку с проектом на вашем компьютере
cd путь/к/вашему/проекту

# Загрузите файлы на сервер
scp -r . root@193.32.188.93:/var/www/derevo/
```

**Вариант В: Загрузка через WinSCP (Windows)**

1. Скачайте и установите WinSCP: https://winscp.net/
2. Подключитесь к серверу:
   - Хост: `193.32.188.93`
   - Пользователь: `root`
   - Пароль: `Zt7Ncu36feJh`
3. Перетащите все файлы проекта в `/var/www/derevo`

---

## 🔐 Шаг 4: Настройка переменных окружения

### 4.1. Создайте файл с настройками
На сервере выполните:
```bash
cd /var/www/derevo
nano .env.production
```

### 4.2. Добавьте в файл следующие строки:

```env
NEXT_PUBLIC_SUPABASE_URL=https://ваш-проект.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш-anon-key
SUPABASE_SERVICE_ROLE_KEY=ваш-service-role-key
TELEGRAM_BOT_TOKEN=ваш-telegram-bot-token
TELEGRAM_CHAT_ID=ваш-telegram-chat-id
```

**Где взять эти значения:**

1. Откройте ваш проект в Supabase: https://supabase.com/dashboard
2. Выберите ваш проект
3. Перейдите в **Settings** → **API**
4. Скопируйте:
   - **Project URL** → `NEXT_PUBLIC_SUPABASE_URL`
   - **anon public** key → `NEXT_PUBLIC_SUPABASE_ANON_KEY`
   - **service_role** key → `SUPABASE_SERVICE_ROLE_KEY` (⚠️ **секретный ключ!**)

5. Для Telegram (если используете):
   - Создайте бота через @BotFather в Telegram
   - Получите токен → `TELEGRAM_BOT_TOKEN`
   - Узнайте ваш Chat ID → `TELEGRAM_CHAT_ID`

### 4.3. Сохраните файл
В редакторе nano:
- Нажмите `Ctrl + O` (сохранить)
- Нажмите `Enter` (подтвердить имя файла)
- Нажмите `Ctrl + X` (выйти)

---

## 🚀 Шаг 5: Запуск приложения

### 5.1. Сделайте скрипт исполняемым
```bash
cd /var/www/derevo
chmod +x deploy.sh
```

### 5.2. Запустите деплой
```bash
./deploy.sh
```

Это займет несколько минут. Скрипт:
- Соберет Docker образ
- Запустит контейнер с приложением
- Проверит, что все работает

Если увидите `✅ Деплой успешно завершен!` - все хорошо!

---

## 🌐 Шаг 6: Настройка веб-сервера (Nginx)

### 6.1. Скопируйте конфигурацию
```bash
cd /var/www/derevo
cp nginx.conf /etc/nginx/sites-available/derevo
```

### 6.2. Включите конфигурацию
```bash
ln -s /etc/nginx/sites-available/derevo /etc/nginx/sites-enabled/
rm /etc/nginx/sites-enabled/default
```

### 6.3. Проверьте конфигурацию
```bash
nginx -t
```

Должно быть: `syntax is ok` и `test is successful`

### 6.4. Перезапустите Nginx
```bash
systemctl restart nginx
```

---

## 🔒 Шаг 7: Настройка SSL (HTTPS) - Рекомендуется

### 7.1. Установите Certbot
```bash
apt install certbot python3-certbot-nginx -y
```

### 7.2. Получите SSL сертификат
```bash
certbot --nginx -d derevo.space -d www.derevo.space
```

Следуйте инструкциям на экране:
- Введите email (для уведомлений)
- Согласитесь с условиями (Y)
- Выберите редирект с HTTP на HTTPS (2)

### 7.3. Проверьте автоматическое обновление
```bash
certbot renew --dry-run
```

---

## ✅ Шаг 8: Проверка работы

### 8.1. Откройте сайт в браузере
Перейдите по адресу: **http://derevo.space** (или https:// после настройки SSL)

Сайт должен открыться!

### 8.2. Проверьте логи (если что-то не работает)
```bash
# Логи приложения
cd /var/www/derevo
docker-compose logs -f

# Логи Nginx
tail -f /var/log/nginx/derevo-access.log
tail -f /var/log/nginx/derevo-error.log
```

---

## 🔄 Обновление сайта (когда нужно обновить код)

### Вариант А: Через Git
```bash
cd /var/www/derevo
git pull
./deploy.sh
```

### Вариант Б: Загрузить новые файлы
```bash
# Загрузите новые файлы на сервер (через SCP или WinSCP)
# Затем на сервере:
cd /var/www/derevo
./deploy.sh
```

---

## 🛠️ Полезные команды

### Просмотр статуса
```bash
cd /var/www/derevo
docker-compose ps
```

### Просмотр логов
```bash
docker-compose logs -f
```

### Перезапуск приложения
```bash
docker-compose restart
```

### Остановка приложения
```bash
docker-compose down
```

### Запуск приложения
```bash
docker-compose up -d
```

---

## ❗ Решение проблем

### Сайт не открывается
1. Проверьте, что контейнер запущен: `docker-compose ps`
2. Проверьте логи: `docker-compose logs`
3. Проверьте Nginx: `systemctl status nginx`
4. Проверьте, что домен указывает на правильный IP

### Ошибки при сборке
1. Проверьте файл `.env.production` - все переменные должны быть заполнены
2. Проверьте логи: `docker-compose logs`

### Проблемы с Nginx
1. Проверьте конфигурацию: `nginx -t`
2. Проверьте логи: `tail -f /var/log/nginx/error.log`

---

## 📞 Если нужна помощь

1. Проверьте логи приложения: `docker-compose logs -f`
2. Проверьте логи Nginx: `tail -f /var/log/nginx/derevo-error.log`
3. Убедитесь, что все переменные окружения заполнены правильно

---

## ✅ Готово!

После выполнения всех шагов ваш сайт будет доступен по адресу:
- **http://derevo.space** (или **https://derevo.space** после настройки SSL)

**Успешного развертывания! 🎉**

