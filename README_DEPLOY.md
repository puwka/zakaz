# 📦 Файлы для деплоя

Проект подготовлен для развертывания на VDS сервере. Все необходимые файлы созданы.

## 📁 Созданные файлы

1. **Dockerfile** - Docker образ для приложения
2. **docker-compose.yml** - Конфигурация Docker Compose
3. **nginx.conf** - Конфигурация Nginx для проксирования
4. **deploy.sh** - Скрипт автоматического деплоя
5. **.dockerignore** - Исключения для Docker сборки
6. **.env.production.example** - Пример файла переменных окружения
7. **DEPLOY.md** - Подробная инструкция по развертыванию
8. **QUICK_START.md** - Краткая инструкция для быстрого старта

## 🎯 Что было изменено

- ✅ Добавлен `output: 'standalone'` в `next.config.js` для оптимизированной сборки
- ✅ Создан Dockerfile для контейнеризации приложения
- ✅ Настроен Docker Compose для удобного управления
- ✅ Подготовлена конфигурация Nginx
- ✅ Создан скрипт автоматического деплоя

## 🚀 Следующие шаги

1. **На сервере:**
   - Установите Docker, Docker Compose и Nginx
   - Загрузите проект на сервер
   - Создайте `.env.production` с реальными значениями
   - Запустите `./deploy.sh`

2. **Настройте домен:**
   - Убедитесь, что домен `derevo.space` указывает на IP `193.32.188.93`
   - Настройте Nginx (см. DEPLOY.md)
   - Установите SSL сертификат через Let's Encrypt

## 📝 Важные замечания

⚠️ **Обязательно:**
- Создайте файл `.env.production` на сервере с реальными значениями переменных окружения
- Получите значения из Supabase Dashboard:
  - `NEXT_PUBLIC_SUPABASE_URL` - URL вашего проекта
  - `NEXT_PUBLIC_SUPABASE_ANON_KEY` - Anon/Public key
  - `SUPABASE_SERVICE_ROLE_KEY` - Service Role key (для админки)

🔒 **Безопасность:**
- Не коммитьте `.env.production` в Git
- Используйте SSL (HTTPS) для production
- Настройте файрвол на сервере

## 📞 Поддержка

Если возникнут проблемы:
1. Проверьте логи: `docker-compose logs -f`
2. Проверьте переменные окружения
3. См. раздел "Решение проблем" в DEPLOY.md

---

**Готово к деплою! 🎉**



