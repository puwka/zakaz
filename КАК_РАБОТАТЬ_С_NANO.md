# 📝 Как работать с редактором nano в веб-консоли

## 🎯 Быстрый старт

### Открыть файл для редактирования:

```bash
nano имя_файла
```

Например:
```bash
nano .env.production
```

---

## ⌨️ Основные команды nano

### Сохранение и выход:

- **`Ctrl + O`** - Сохранить файл
  - После нажатия появится запрос имени файла
  - Нажмите **`Enter`** для подтверждения
  - Файл будет сохранен

- **`Ctrl + X`** - Выйти из редактора
  - Если есть несохраненные изменения, nano спросит, сохранить ли их
  - Нажмите **`Y`** (да) или **`N`** (нет)

### Редактирование текста:

- **`Ctrl + K`** - Вырезать строку (где находится курсор)
- **`Ctrl + U`** - Вставить вырезанную строку
- **`Backspace`** - Удалить символ перед курсором
- **`Delete`** - Удалить символ после курсора
- **Стрелки** - Перемещение курсора

### Поиск и замена:

- **`Ctrl + W`** - Поиск текста
  - Введите текст для поиска
  - Нажмите Enter
  - Используйте `Alt + W` для поиска следующего вхождения

- **`Ctrl + \`** - Замена текста
  - Введите текст для поиска
  - Введите текст для замены
  - Подтвердите замену

### Другие полезные команды:

- **`Ctrl + G`** - Показать справку (все команды)
- **`Ctrl + C`** - Показать текущую позицию курсора
- **`Ctrl + _`** - Перейти на определенную строку

---

## 📋 Как вставлять текст в nano через веб-консоль

### Способ 1: Правый клик → Paste (РЕКОМЕНДУЕТСЯ) ⭐

1. **Скопируйте текст** в буфер обмена:
   - Выделите текст в браузере
   - Нажмите `Ctrl + C` (или правый клик → Copy)

2. **В веб-консоли:**
   - Убедитесь, что курсор находится в нужном месте в nano
   - **Кликните правой кнопкой мыши** в окне nano
   - Выберите **"Paste"** (Вставить) из контекстного меню
   - Текст вставится в текущую позицию курсора

**Это самый надежный способ!**

---

### Способ 2: Ctrl + Shift + V

1. **Скопируйте текст** в буфер обмена (`Ctrl + C`)

2. **В веб-консоли:**
   - Убедитесь, что курсор находится в нужном месте в nano
   - Нажмите **`Ctrl + Shift + V`**
   - Текст вставится в nano

**Примечание:** В некоторых браузерах это может не работать, используйте Способ 1.

---

### Способ 3: Ввод вручную

Если вставка не работает:

1. **Введите текст вручную** посимвольно
2. Используйте **стрелки** для навигации
3. Используйте **`Backspace`** для удаления

---

## 💡 Полезные советы

### 1. Просмотр доступных команд

Внизу экрана nano всегда показаны доступные команды:
```
^G Get Help  ^O Write Out  ^W Where Is  ^K Cut Text  ^J Justify
^X Exit      ^R Read File  ^\ Replace   ^U Paste     ^T To Spell
```

- **`^`** означает **`Ctrl`**
- Например, `^O` = `Ctrl + O`

### 2. Многострочная вставка

Если нужно вставить несколько строк:
1. Скопируйте весь текст (включая переносы строк)
2. Вставьте через правый клик → Paste
3. Все строки вставятся сразу

### 3. Редактирование существующего файла

Если файл уже существует:
1. Откройте его: `nano имя_файла`
2. Текст уже будет в редакторе
3. Используйте стрелки для навигации
4. Введите новый текст или удалите старый
5. Сохраните: `Ctrl + O`, `Enter`, `Ctrl + X`

### 4. Создание нового файла

Если файл не существует:
1. Откройте: `nano новый_файл.txt`
2. Nano создаст новый файл
3. Введите содержимое
4. Сохраните: `Ctrl + O`, `Enter`, `Ctrl + X`

### 5. Отмена изменений

Если вы случайно что-то изменили:
- **`Ctrl + X`** - выйти
- На вопрос "Save modified buffer?" нажмите **`N`** (нет)
- Изменения не сохранятся

---

## 📝 Пример: Создание файла .env.production

### Шаг 1: Откройте файл

```bash
cd /var/www/derevo
nano .env.production
```

### Шаг 2: Вставьте содержимое

**Текст для вставки:**
```
NEXT_PUBLIC_SUPABASE_URL=https://ваш-проект.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш-anon-key
SUPABASE_SERVICE_ROLE_KEY=ваш-service-role-key
TELEGRAM_BOT_TOKEN=ваш-telegram-bot-token
TELEGRAM_CHAT_ID=ваш-telegram-chat-id
```

**Как вставить:**
1. Скопируйте весь текст выше
2. В веб-консоли кликните **правой кнопкой мыши** в окне nano
3. Выберите **"Paste"**
4. Текст вставится

### Шаг 3: Замените значения

1. Используйте **стрелки** для перемещения курсора
2. Замените `ваш-проект.supabase.co` на реальный URL
3. Замените `ваш-anon-key` на реальный ключ
4. И так далее...

### Шаг 4: Сохраните файл

1. Нажмите **`Ctrl + O`** (сохранить)
2. Нажмите **`Enter`** (подтвердить имя файла)
3. Нажмите **`Ctrl + X`** (выйти)

**Готово!** Файл сохранен.

---

## ❗ Частые проблемы

### Проблема: Не могу вставить текст (вставка не работает)

**Если правый клик и Ctrl+Shift+V не работают, используйте альтернативные способы:**

#### Решение 1: Создание файла через echo (РЕКОМЕНДУЕТСЯ) ⭐

Вместо nano используйте команду `echo` для создания файла:

```bash
# Создайте файл одной командой
cat > .env.production << 'EOF'
NEXT_PUBLIC_SUPABASE_URL=https://ваш-проект.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш-anon-key
SUPABASE_SERVICE_ROLE_KEY=ваш-service-role-key
TELEGRAM_BOT_TOKEN=ваш-telegram-bot-token
TELEGRAM_CHAT_ID=ваш-telegram-chat-id
EOF
```

**Как использовать:**
1. Скопируйте команду выше
2. Замените `ваш-проект.supabase.co` и другие значения на реальные
3. Вставьте команду в консоль (правый клик → Paste)
4. Нажмите Enter
5. Файл будет создан автоматически!

**Проверка:**
```bash
cat .env.production
```

#### Решение 2: Создание файла построчно через echo

```bash
# Создайте файл построчно
echo "NEXT_PUBLIC_SUPABASE_URL=https://ваш-проект.supabase.co" > .env.production
echo "NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш-anon-key" >> .env.production
echo "SUPABASE_SERVICE_ROLE_KEY=ваш-service-role-key" >> .env.production
echo "TELEGRAM_BOT_TOKEN=ваш-telegram-bot-token" >> .env.production
echo "TELEGRAM_CHAT_ID=ваш-telegram-chat-id" >> .env.production
```

**Важно:** 
- Первая строка использует `>` (создает/перезаписывает файл)
- Остальные строки используют `>>` (добавляют в конец файла)

#### Решение 3: Редактирование через sed (если файл уже создан)

Если файл уже создан, можно редактировать через sed:

```bash
# Заменить значение в файле
sed -i 's|ваш-проект.supabase.co|реальный-url.supabase.co|g' .env.production
```

#### Решение 4: Использование vi/vim (если установлен)

```bash
vi .env.production
```

**Основные команды vi:**
- Нажмите `i` для входа в режим вставки
- Введите текст
- Нажмите `Esc` для выхода из режима вставки
- Введите `:wq` и Enter для сохранения и выхода

#### Решение 5: Загрузка файла через WinSCP или SCP

Если вставка в nano не работает, загрузите файл с вашего компьютера:

1. **Создайте файл на вашем компьютере:**
   - Создайте файл `.env.production` в текстовом редакторе
   - Заполните его значениями
   - Сохраните

2. **Загрузите через WinSCP:**
   - Подключитесь к серверу (193.32.188.93)
   - Перетащите файл в `/var/www/derevo/`

3. **Или через SCP (из терминала на вашем компьютере):**
   ```bash
   scp .env.production root@193.32.188.93:/var/www/derevo/
   ```

#### Решение 6: Ввод вручную в nano

Если ничего не помогает, введите текст вручную:
1. Откройте nano: `nano .env.production`
2. Введите каждую строку вручную
3. Используйте стрелки для навигации
4. Сохраните: `Ctrl + O`, `Enter`, `Ctrl + X`

### Проблема: Случайно закрыл nano без сохранения

**Решение:**
- Откройте файл заново: `nano имя_файла`
- Введите содержимое снова
- На этот раз сохраните: `Ctrl + O`, `Enter`

### Проблема: Не знаю, какую команду использовать

**Решение:**
- Нажмите **`Ctrl + G`** - откроется справка со всеми командами
- Внизу экрана всегда показаны доступные команды

### Проблема: Хочу отменить изменения

**Решение:**
- Нажмите **`Ctrl + X`** для выхода
- На вопрос "Save modified buffer?" нажмите **`N`** (нет)
- Изменения не сохранятся

---

## ✅ Готово!

Теперь вы знаете, как работать с nano в веб-консоли. 

**Главное запомнить:**
- **`Ctrl + O`** - сохранить
- **`Ctrl + X`** - выйти
- **Правый клик → Paste** - вставить текст

**Удачи! 🚀**

